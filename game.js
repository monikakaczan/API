class Game {
    static submitEntry = (entry) => {
        const record = {
            name: entry.name,
            word: entry.word,
            points: 0
        }

    }

    static isPalindrome = (record) => { 

    }

    static getScore = (record) => {

    }

    static getScores = () => {
        
    }
}

export default Game;